/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "127.0.0.1",
      "shark-app-jfgip.ondigitalocean.app",
      "usercontent.one",
    ],
  },
};

export default nextConfig;
